<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.kevindwitama.dotamarketplace">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/maps_key" />

        <activity android:name="com.github.kevindwitama.dotamarketplace.ui.activities.SellerLocationActivity" />
        <activity android:name="com.github.kevindwitama.dotamarketplace.ui.activities.HistoryActivity" />
        <activity android:name="com.github.kevindwitama.dotamarketplace.ui.activities.TopUpActivity" />
        <activity android:name="com.github.kevindwitama.dotamarketplace.ui.activities.MainFormActivity" />
        <activity android:name="com.github.kevindwitama.dotamarketplace.ui.activities.BuyItemActivity" />
        <activity android:name="com.github.kevindwitama.dotamarketplace.ui.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>